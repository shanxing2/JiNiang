发弹幕
	异步启动{弹幕回收器，重复弹幕发送器，发弹幕发送器}

发送弹幕循环
	EnsureHandledRepetitiveDanmu函数：
	existSentRepeatitiveDanmu=检查已发送列表是否有同样的
		有
			阻塞模式	
				定义临时时间变量 = existSentRepeatitiveDanmu.SentRepeatitiveDanmu.SentTicks
				while existSentRepeatitiveDanmu.Exist
					existRepetitiveDanmu=检查重复列表是否有同样的
					有
						临时时间变量 = 重复弹幕的时间+可发重复弹幕间隔
					无
						临时时间变量 +=可发重复弹幕间隔
						
					创建一条重复信息，内容\来源一样，时间为 上次发送重复弹幕时间+可发送重复弹幕间隔 ,计数为0
					加到重复弹幕列表
					尝试获取下条待发送弹幕
						成功
							existSentRepeatitiveDanmu=检查已发送列表是否有同样的							
						失败
							退出循环
				end while						
				
				如果计数大于1 时间-可发送重复弹幕间隔
				加到重复弹幕列表									
					
			合并模式
				创建一条临时重复信息，内容\来源一样，时间为 上次发送重复弹幕时间+可发送重复弹幕间隔 ,计数为0
				
				while existSentRepeatitiveDanmu.Exist
					existRepetitiveDanmu=检查重复列表是否有同样的
						有
							existRepetitiveDanmu.Danmu.计数+1	
						无
							临时重复信息计数+1
						尝试获取下条待发送弹幕
							成功
								existSentRepeatitiveDanmu=检查重复列表是否有同样的							
							失败
								退出循环							
				end while								
				
				如果临时重复信息计数等于0，退出
				如果临时重复信息计数大于1 时间-可发送重复弹幕间隔				
				加到重复弹幕列表
					
		无
			退出函数
			
			
	发送	进入已发送列表	反馈给用户
	
	
	
	检查重复列表是否有同样的函数：
		检查重复信息列表个数是否为0 
		是
			返回 假
		如果文本一样 返回 真

		尝试查找重复的弹幕
		返回查找结果
		
		